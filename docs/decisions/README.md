# QRSIP — ADR index
#
# Architectural Decision Records (ADRs) capture material architectural
# decisions, the context, alternatives, consequences, and status.
#
# Format: ADR-000N-short-title.md
#
# Each ADR:
# - Context
# - Decision
# - Alternatives considered
# - Consequences
# - Status
#
# ADRs live here so decisions are auditable and revisited when needed.
#
# Initial ADRs:
#
# ADR-0001 — Python packaging and dependency management approach
# ADR-0002 — Storage port first, PostgreSQL introduced when required
# ADR-0003 — CLI-first presentation, FastAPI deferred
# ADR-0004 — Licensing strategy (TBD)
# ADR-0005 — Signal/fill timing convention
# ADR-0006 — Numerical tolerance policy
# ADR-0007 — P01 contract fixtures and boundaries
# ADR-0008 — Deterministic experiment rerun policy
# ADR-0009 — Toolchain selection (ruff, mypy, pytest)
# ADR-0010 — Docker usage and scope
